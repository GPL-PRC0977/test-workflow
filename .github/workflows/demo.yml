name: Hello Demo

on:
  push:
    branches:
      - main

jobs:
  demo:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Greetings
        shell: bash
        run: |
          echo "-----------------------------"
          echo "HELLO DEMO WORKFLOW IS RUNNING"
          echo "Current directory:"
          pwd
          echo "Files:"
          ls -al
          echo "-----------------------------"
